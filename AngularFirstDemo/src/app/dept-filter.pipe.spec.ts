import { DeptFilterPipe } from './dept-filter.pipe';

describe('DeptFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DeptFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
