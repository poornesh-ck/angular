import { DeptDirectiveDirective } from './dept-directive.directive';

describe('DeptDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DeptDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
